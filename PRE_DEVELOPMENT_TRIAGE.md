# 開発前トリアージ — PII リスク事前検出

新規プロジェクト着手前に **必ず実施**してください。
Claude Code セッション内で `/triage` を叩くと対話で進められます。

---

## 3 つの質問（1 つでも「はい」なら → L3 / エンジニア相談必須）

### Q1. 個人を特定できる情報を取り扱いますか？

下のうち、1 つでも該当するなら「はい」:

- [ ] お客様の氏名・メール・電話・住所
- [ ] お客様の顔写真・身分証画像・名刺画像
- [ ] お客様の通話録音・声
- [ ] お客様の問い合わせ内容・対応履歴
- [ ] 社員の氏名 + 給与・評価・勤怠・健康情報
- [ ] クレジット情報・銀行口座
- [ ] パスワード・ログイン認証情報
- [ ] 個人情報や社内機密にアクセスできる API キー（read-only でも該当）

API の判定基準 — **「そのキーで何が読める / 書けるか」で考える**:
- **該当例**: Slack の DM 履歴取得 / Drive 全社閲覧 / Gmail 読取 / 顧客 DB / HR / 売上データへのアクセス、書込権限を持つ API
- **非該当例**: Slack 通知のみ（chat:write）/ Google Calendar 自分の予定参照 / 公開ドキュメント取得 / 汎用 webhook（自分宛て）

※ 商品写真・UI スクショ・効果音は対象外
※ 社員番号「だけ」（氏名・給与と紐づけない単独利用）は対象外
※ **迷ったら「はい」（L3 に倒す）**

### Q2. 社外の人（お客様）が直接使うシステムですか？

- 顧客向け Web フォーム / 予約サイト / 顧客アプリ → **はい**
- 社内のみで使う → いいえ

### Q3. お金が絡みますか？

- 決済 / 振込 / 送金 / クレジット情報の通り道 → **はい**
- 金銭にすぐ換算できる権限（割引コード発行 / ポイント付与 / 在庫増減）→ **はい**

---

## 判定

| 結果 | レベル | 次のアクション |
|---|---|---|
| すべて「いいえ」 | **L1** | `.security-level` に `1` を書く → AI 事業部内で進めて OK |
| 1 つでも「はい」 | **L3** | `.security-level` に `3` を書く → エンジニア相談必須 |

---

## L3 の場合の必須プロセス

1. Slack `#ai-jigyobu-engineer-consult` で相談する
2. AI 事業部のエンジニア担当から設計助言をもらう
3. `docs/engineer-consultation.md` に**必須 6 項目を埋める**:
   - 対象データ
   - 外部送信先
   - 必要権限
   - 想定事故
   - 許可範囲
   - 有効期限（最大 6 ヶ月推奨）
4. 上記が揃ってから開発開始（CI が gate）

---

## 再トリアージが必要な変更

以下のいずれかが PR 差分に含まれる場合、`/triage` を**再実施**して `.security-level` を更新:

- 新規 API 接続（`.env` に新規 KEY 追加 / OAuth scope 追加）
- 外部送信先追加（Slack webhook / Twilio / Stripe / SendGrid / AWS SES 等）
- 書込権限の追加（read-only → write）
- DB スキーマ変更（テーブル追加 / カラム追加）

CI が差分を検知し、`docs/triage-record.md` が古いと fail させます。

---

## 例外運用

- L3 案件で「許可範囲」を超える変更が必要になったら **再相談・再承認**
- 例外を許す場合は **必ず期限を切る**（無期限は禁止）
- 期限切れは audit / CI で検知される
